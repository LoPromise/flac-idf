This is a wrapper for the complete Flac Project to compile with ESP-IDF
just add as a component and compile.
Note for Pulling: 
<code>git clone --recursive https://github.com/LoPromise/flac-idf.git</code>
